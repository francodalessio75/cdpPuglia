export interface LanguageData{
    sections:{
        global:{
            languageLabel:string;
            italian:string;
            english:string;
            submitButton:string;
            discardButton:string;
        },
        login:{
            pageTitle:string;
            pageDescription:string;
        }
    }
}